######################################
##### TEMPLATE - Critical Device #####
######################################

- sensor:
  - name: "Mac Mini Server"
    unique_id: mac_mini_server
    state: "{% if states.device_tracker.mac_mini_server %}
      {% if is_state('device_tracker.mac_mini_server', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"

  - name: "Synology NAS"
    unique_id: synology_nas
    state: "{% if states.device_tracker.synology_nas %}
      {% if is_state('device_tracker.synology_nas', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"

  - name: "Kitchen Tablet"
    unique_id: kitchen_tablet
    state: "{% if states.device_tracker.kitchen_fire_tablet %}
      {% if is_state('device_tracker.kitchen_fire_tablet', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"
    
  - name: "HDHomeRun Extend"
    unique_id: hdhomerun_extend
    state: "{% if states.device_tracker.hdhomerun_extend %}
      {% if is_state('device_tracker.hdhomerun_extend', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"
      
  - name: "Logitech Harmony Hub"
    unique_id: logitech_harmony_hub
    state: "{% if states.device_tracker.logitech_harmony_hub %}
      {% if is_state('device_tracker.logitech_harmony_hub', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"
      
  - name: "Yamaha Receiver"
    unique_id: yamaha_reciever
    state: "{% if states.device_tracker.yamaha_rx_v675 %}
      {% if is_state('device_tracker.yamaha_rx_v675', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"
      
  - name: "Rear Porch Camera"
    unique_id: rear_porch_camera
    icon: mdi:cctv
    state: "{% if states.device_tracker.rear_porch_camera %}
      {% if is_state('device_tracker.rear_porch_camera', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"
      
  - name: "Side Patio Camera"
    unique_id: side_patio_camera
    icon: mdi:cctv
    state: "{% if states.device_tracker.side_patio_camera %}
      {% if is_state('device_tracker.side_patio_camera', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"
      
  - name: "Garage Camera"
    unique_id: garage_camera
    icon: mdi:cctv
    state: "{% if states.device_tracker.garage_camera %}
      {% if is_state('device_tracker.garage_camera', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"
      
  - name: "Doorbell Camera"
    unique_id: doorbell_camera
    icon: mdi:cctv
    state: "{% if states.device_tracker.doorbell %}
      {% if is_state('device_tracker.doorbell', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"

  - name: "Living Room Camera"
    unique_id: living_room_camera
    icon: mdi:cctv
    state: "{% if states.device_tracker.living_room_camera %}
      {% if is_state('device_tracker.living_room_camera', 'home') %}
        Online
      {% else %}
        Offline
      {% endif %}
      {% else %}
        Unknown
      {% endif %}"