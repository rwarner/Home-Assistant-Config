toggle_dresser_lamp:
  sequence:
    service: light.toggle
    entity_id: 
      - light.master_bedroom
    data_template:
      brightness: >
        {% if now().hour > 22 %}
          80
        {% elif now().hour < 7 %}
          80
        {% else %}
          255
        {% endif %}
